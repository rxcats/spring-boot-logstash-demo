docker build -t docker-logstash .;docker stop docker-logstash;docker rm docker-logstash;docker run -d -p 5000:5000 --restart=always --name docker-logstash docker-logstash;docker logs -f docker-logstash